---
// Panel de administración - Vista de todas las reservas
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Admin - Reservas">
  <div class="container mx-auto px-4 py-8">
    <!-- Header -->
    <div class="flex justify-between items-center mb-8">
      <div>
        <h1 class="text-4xl font-display font-bold text-gradient">Panel de Administración</h1>
        <p class="text-gray-400 mt-2">Gestiona todas las reservas de la barbería</p>
      </div>
      <a href="/" class="btn btn-outline">
        ← Volver al inicio
      </a>
    </div>

    <!-- Filtros -->
    <div class="card mb-6">
      <div class="card-body">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
          <div class="form-control">
            <label class="label">
              <span class="label-text">Fecha desde</span>
            </label>
            <input type="date" id="fecha-desde" class="input" />
          </div>
          <div class="form-control">
            <label class="label">
              <span class="label-text">Fecha hasta</span>
            </label>
            <input type="date" id="fecha-hasta" class="input" />
          </div>
          <div class="form-control">
            <label class="label">
              <span class="label-text">Estado</span>
            </label>
            <select id="filtro-estado" class="select">
              <option value="todas">Todas</option>
              <option value="pendiente">Pendientes</option>
              <option value="confirmada">Confirmadas</option>
              <option value="completada">Completadas</option>
              <option value="cancelada">Canceladas</option>
            </select>
          </div>
        </div>
        <button id="btn-filtrar" class="btn btn-primary mt-4">
          Filtrar Reservas
        </button>
      </div>
    </div>

    <!-- Estadísticas rápidas -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-6">
      <div class="card bg-primary/10 border-primary/30">
        <div class="card-body">
          <div class="text-xs text-gray-400">Total Reservas</div>
          <div class="text-3xl font-bold text-primary" id="stat-total">0</div>
        </div>
      </div>
      <div class="card bg-blue-500/10 border-blue-500/30">
        <div class="card-body">
          <div class="text-xs text-gray-400">Pendientes</div>
          <div class="text-3xl font-bold text-blue-400" id="stat-pendientes">0</div>
        </div>
      </div>
      <div class="card bg-green-500/10 border-green-500/30">
        <div class="card-body">
          <div class="text-xs text-gray-400">Confirmadas</div>
          <div class="text-3xl font-bold text-green-400" id="stat-confirmadas">0</div>
        </div>
      </div>
      <div class="card bg-purple-500/10 border-purple-500/30">
        <div class="card-body">
          <div class="text-xs text-gray-400">Completadas</div>
          <div class="text-3xl font-bold text-purple-400" id="stat-completadas">0</div>
        </div>
      </div>
    </div>

    <!-- Tabla de reservas -->
    <div class="card">
      <div class="card-body">
        <div id="loading" class="text-center py-8">
          <div class="loading loading-spinner loading-lg"></div>
          <p class="mt-4 text-gray-400">Cargando reservas...</p>
        </div>
        
        <div id="reservas-container" class="hidden">
          <div class="overflow-x-auto">
            <table class="table w-full">
              <thead>
                <tr class="border-b border-primary/20">
                  <th>Fecha</th>
                  <th>Hora</th>
                  <th>Cliente</th>
                  <th>Teléfono</th>
                  <th>Servicio</th>
                  <th>Estado</th>
                  <th>Acciones</th>
                </tr>
              </thead>
              <tbody id="reservas-tbody">
                <!-- Contenido dinámico -->
              </tbody>
            </table>
          </div>
        </div>

        <div id="no-reservas" class="hidden text-center py-12">
          <div class="text-6xl mb-4">📅</div>
          <p class="text-xl text-gray-400">No hay reservas para mostrar</p>
        </div>
      </div>
    </div>
  </div>
</Layout>

<script>
  import { initAdminPanel } from '../../components/AdminPanel';
  
  document.addEventListener('DOMContentLoaded', () => {
    initAdminPanel();
  });
</script>

<style>
  .table {
    border-collapse: collapse;
  }
  
  .table th {
    padding: 1rem;
    text-align: left;
    font-weight: 600;
    color: var(--barber-gold);
  }
  
  .table td {
    padding: 1rem;
    border-bottom: 1px solid rgba(212, 175, 55, 0.1);
  }
  
  .table tbody tr:hover {
    background: rgba(212, 175, 55, 0.05);
  }
</style>
